# --- Web Framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.1

# --- Data Validation & Settings ---
pydantic>=2.7.1
pydantic-settings>=2.2.1

# --- Vector Database ---
qdrant-client>=1.9.1

# --- AI & Embeddings ---
cohere>=5.5.3
fastembed>=0.2.7
asyncio>=3.4.3

# --- Document Parsing (ETL Pipeline) ---
pymupdf>=1.24.0         # PDF parsing (also known as fitz)
python-docx>=1.1.0      # DOCX parsing
python-multipart>=0.0.9 # Multipart file uploads in FastAPI

# --- Security & Auth ---
python-jose[cryptography]>=3.3.0  # JWT token signing & verification
passlib[bcrypt]>=1.7.4            # Password hashing

# --- Rate Limiting ---
slowapi>=0.1.9

# --- Session Memory (In-process, upgradeable to Redis) ---
cachetools>=5.3.3

# --- Logging ---
loguru>=0.7.2